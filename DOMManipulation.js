// DOM Manipulation

// 1. querySelector - The modern, flexible way (most popular)

const button = document.querySelector(.my-button');
const heading = document.querySelector('h1');
const item = document.querySelector('#special-item');

// 2. getElementById - Specific for IDs (older but still useful)
const mainArea = document.getElementById('main');

// 3. querySelectorAll - Select multiple elements at once
const buttons = document.querySelectorAll('button');

